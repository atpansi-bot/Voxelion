name: Build VOXELION Android APK

on:
  push:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup .NET 8
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '8.0.x'

      - name: Setup Java 17
        uses: actions/setup-java@v5
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Install Android workload
        run: |
          dotnet workload install android --skip-manifest-update || true
          dotnet workload list

      - name: Restore
        run: |
          dotnet restore Voxelion.Android/Voxelion.Android.csproj \
            /p:CheckEolTargetFramework=false

      - name: Build & Publish APK
        run: |
          dotnet publish Voxelion.Android/Voxelion.Android.csproj \
            -c Release \
            -f net8.0-android \
            -o ./output \
            /p:ApplicationId=com.voxelion.app \
            /p:ApplicationTitle=VOXELION \
            /p:CheckEolTargetFramework=false \
            /p:EmbedAssembliesIntoApk=true

      - name: List output
        run: find output -type f | head -50

      - name: Verify APK
        run: |
          APK=$(find output -name "*.apk" | head -1)
          echo "APK=$APK"
          if [ -z "$APK" ]; then echo "No APK produced"; exit 1; fi
          unzip -l "$APK" | tee apk-list.txt
          if unzip -p "$APK" res/layout/activity_main.xml 2>/dev/null | strings | grep -qi "Hello"; then
            echo "FAIL: Hello Android layout still present"
            exit 1
          fi
          echo "OK: APK built"

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: voxelion-android-apk
          path: |
            output/**/*.apk
            output/**/*-Signed.apk
